<div>
	<p>
		<span class="port-code-suggest">
			<%= port_code %>
		</span> 
		<%= value %>
	</p>
</div>
<div>
	<img src="<%= flag %>" />
		<% for(var i = 0; i < transport_icons.length; i++){ %>
			<img src="<%= transport_icons[i] %>" />
	<% } %>
</div>	